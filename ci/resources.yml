resources:
  - name: ucfs-claimant-kafka-consumer-pr
    type: pull-request
    source:
      repository: dwp/ucfs-claimant-kafka-consumer
      access_token: ((dataworks-secrets.concourse_github_pat))
    webhook_token: ((dataworks.concourse_github_webhook_token))
    check_every: 720h

  - name: ucfs-claimant-kafka-consumer
    type: git
    source:
      branch: master
      uri: https://github.com/dwp/ucfs-claimant-kafka-consumer.git
      access_token: ((dataworks-secrets.concourse_github_pat))
    webhook_token: ((dataworks.concourse_github_webhook_token))
    check_every: 720h
